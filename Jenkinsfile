pipeline { 
    agent any 

    stages { 
        stage('STAGE 00'){ 
            steps{
                bash '''#!/bin/bash
                echo "Pipeline Usando Jenkinsfile"
            }
        }

        stage('STAGE 01'){ 
            steps{
                echo "Pipeline Usando Jenkinsfile"
            }
        } 
    } 
} 
